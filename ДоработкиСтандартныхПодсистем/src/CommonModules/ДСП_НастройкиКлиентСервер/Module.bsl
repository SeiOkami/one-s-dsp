// @strict-types

#Область ПрограммныйИнтерфейс

// см. ДСП_Настройки.НастройкиРасширения
Функция НастройкиРасширения(ИзКэша = Истина) Экспорт
	
	#Если Сервер Тогда
		Возврат ДСП_Настройки.НастройкиРасширения(ИзКэша);
	#Иначе
		Если ИзКэша Тогда
			Возврат ДСП_НастройкиКлиентПовтИсп.НастройкиРасширения();
		Иначе
			Возврат ДСП_НастройкиВызовСервера.НастройкиРасширения();
		КонецЕсли;
	#КонецЕсли
	
КонецФункции

#КонецОбласти